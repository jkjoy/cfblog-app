# Legacy Expo Archive

This directory contains the previous Expo/React Native implementation of the CFBlog client.

It is preserved only as a reference during the Flutter migration. New development should happen in the Flutter app at the repository root.

Historical notes are kept in `README.expo.md`.
